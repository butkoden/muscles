# Cursor Rules For Muscles

- Start each session with `muscles capabilities --json`.
- Use `muscles inspect --json` before changing architecture-sensitive parts.
- Prefer `muscles generate` over manual scaffolding.
- Run `muscles doctor` and `muscles test` after changes.
